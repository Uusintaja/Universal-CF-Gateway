name = "universal-cf-gateway-mvp"
main = "src/index.ts"
compatibility_date = "2024-07-01"
compatibility_flags = ["nodejs_compat"]

# Phase 1 scope per DESIGN-DOC Implementation Plan:
# - Input I/O + Decoder + Router + render + transmit (direct, no DO/Queues/KV)
# Phase 1 can work without DO/Queues/KV (pure sync)
# DO (Phase 2), Queues + KV (Phase 3) kept commented as placeholders

# [[queues.producers]]
# queue = "slack-webhook"
# binding = "Q_SLACK"
# [[queues.consumers]]
# queue = "slack-webhook"
# max_batch_size = 5
# max_batch_timeout = 60
# max_retries = 3

# [durable_objects]
# bindings = [{ name = "COORDINATOR", class_name = "CoordinatorDO" }]
# [[migrations]]
# tag = "v1"
# new_classes = ["CoordinatorDO"]

# [[kv_namespaces]]
# binding = "KV"
# id = "local-kv-placeholder" # Real id required for deploy, Phase 3 only

